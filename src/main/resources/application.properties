spring.application.name=crudbasic

# --- Database Connection ---
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=Ventas;encrypt=false
spring.datasource.username=sa
spring.datasource.password=dani12345

# Driver
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# --- Hibernate / JPA ---
#  create crear la base de datos si no existe, update actualiza la base de datos segun las entidades, validate valida que la
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
